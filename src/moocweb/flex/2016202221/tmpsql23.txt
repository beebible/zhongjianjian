SELECT SNAME
FROM STUDENT
WHERE NOT EXISTS
(SELECT *
FROM COURSE
WHERE NOT EXISTS
	(SELECT *
	 FROM COURSE
	 WHERE NOT EXISTS
		(SELECT * 
		FROM SC 
		WHERE SNO = STUDENT.SNO 
		AND CNO = COURSE.CNO
		)
	)
)&exit